# プリズムボード (Prism Board) 設計書 v1.0

Prismaera のミニゲーム。オートチェス(TFT / Chess Rush)準拠の **PvE/ゴースト型オートバトラー**。
ガチャで集めたキャラをショップで引き・盤面に並べ・自動戦闘で **12ラウンド生存** を目指す。**1試合 約5〜7分**。

> ベンチマーク: Auto Chess/TFT(30-40分・8人PvP・ショップ→配置→自動戦闘・3体合成・シナジー・利息経済) / Chess Rush(ターボ10分・XP/Gold 2倍で高速化) を参照。
> 本作は **リアルタイムPvP不要**(ゴースト=他者保存編成 or AI と戦う)で Firebase 完結、短時間(5-7分)に圧縮。

---

## 1. 設計方針(確定事項)

| 項目 | 決定 |
|---|---|
| 対戦形式 | **PvE/ゴースト戦**(AI編成 + 他プレイヤー保存編成。リアルタイム同期なし) |
| 勝敗 | **HP制サバイバル**(12ラウンド、HP0で終了) |
| シナジー | **派閥(オリジン)× ロール(クラス)の2軸** |
| 試合時間 | 約5〜7分(目標6分) |
| データ保持 | **Firebase=アカウント保持**(localStorageはキャッシュのみ) |

---

## 2. 試合フロー

全 **12ラウンド**。各ラウンド = 準備フェーズ + 自動戦闘フェーズ。

| ラウンド | 種別 | 内容 | 想定秒数 |
|---|---|---|---|
| R1 | PvE(導入) | 弱クリープ。チュートリアル誘導 | 準備10s + 戦闘10s |
| R2-3 | PvE(報酬) | クリープ撃破でゴールド/アイテム。負けてもダメージ0 | 各 準備10s + 戦闘12s |
| R4-7 | ゴースト/AI | 難易度カーブ(弱→中) | 各 準備12s + 戦闘15s |
| R8-11 | ゴースト/AI | 中→強 | 各 準備12s + 戦闘18s |
| R12 | ボス | ボス級ゴースト or 専用強敵。勝利でクリアボーナス | 準備15s + 戦闘20s |

合計 ≒ 5.5〜6.5分(戦闘は全滅で早期決着あり)。**準備フェーズ中に「次へ」で短縮可**(待ち時間最小化)。

---

## 3. 経済(Chess Rush ターボ準拠で高速化)

- **開始**: HP=30 / ゴールド=3 / レベル=2(配置2体)。
- **毎ラウンド収入** = 基本5 + 利息(所持10ごと+1、最大+4) + 連勝/連敗ボーナス(2連=+1, 4連=+2)。
- **XP**: 毎ラウンド自動 +2 / 購入(4G で +4)。
- **レベル(=配置上限)**: 2→8。必要累計XP(圧縮): L3=2, L4=8, L5=18, L6=34, L7=54, L8=80。
- **リロール**(ショップ更新)= 2G。

---

## 4. ユニット(既存POOL ~242体を流用)

- ユニット = Prismaera キャラ。`img` はそのまま使用。
- **ティア → ショップコスト & 排出率**:

| ティア | コスト | プール枚数(各ユニット) | 主な排出レベル |
|---|---|---|---|
| R | 1G | 多 | L2〜 |
| SR | 2G | 中 | L3〜 |
| SSR | 3G | 中 | L4〜 |
| UR | 4G | 少 | L6〜 |
| LR | 5G | 極少 | L7〜 |

- ショップは5枠。レベルで高ティアの排出率が上がる(TFT準拠の確率テーブル、§10)。
- `basePower` → HP/ATK に換算(§10)。
- **3体合成 → ★2(2.2倍)、★2×3 → ★3(4倍)**(オートチェス定石)。

---

## 5. シナジー(派閥 × ロールの2軸)

### 5-1. 派閥(オリジン)= 20種

同派閥ユニットを **2/4/6体** 並べると段階ボーナス。**初期実装は優先8派閥に固有効果**、残りは汎用効果(2体=全体ATK+8% / 4体=+18% / 6体=+30%)から段階拡張。

| id | 派閥 | テーマ | 効果案(2/4/6) | 優先 |
|---|---|---|---|---|
| silver | 銀霜王国 | 氷/防御 | 被ダメ-10/20/30%、4+で敵に低確率 凍結 | ★ |
| redwing | 紅翼皇家 | 炎/火力 | ATK+15/30/50%、6で炎上(継続ダメ) | ★ |
| church | 白焔教会 | 回復/聖盾 | 開幕シールド / 毎秒回復 段階UP | ★ |
| wolf | 月牙狼族 | 連撃/速度 | 攻撃速度+15/30/50% | ★ |
| dragon | 紫竜王国 | 貫通/単体高火力 | 竜のユニットがブレス(範囲)、段階で威力UP | ★ |
| academy | 星霊学院 | 魔力/スキル加速 | スキル発動短縮、魔力+ | ★ |
| yakai | 夜焔郷・影衆 | 暗殺/闇 | 最後列(敵後衛)へ追加ダメージ | ★ |
| crimson | 紅玉海賊団 | 強奪/経済 | 勝利時ゴールド+、撃破でドロップ | ★ |
| genso | 原虹・観測者 | 万能/オーラ | 全シナジーを +1体分カウント(汎用ブースト) | 後 |
| rulers/tower/forest/seventh/aquasis/sahar/niiruru/zenonia/darkmoon/liora/shrines | — | — | まず汎用効果、順次固有化 | 後 |

### 5-2. ロール(クラス)= 6種(初期)

各キャラに1クラスを付与(`title`/`caption`から導出 or 手動タグ、§11)。同クラス **2/3/4体** で効果。

| クラス | 役割 | 効果案(2/3/4) |
|---|---|---|
| 守護(重装) | 前衛タンク | 最大HP+20/40/60% |
| 剣士 | 前衛アタッカー | ATK+12/24/40% |
| 魔導 | 後衛魔法 | スキル威力+20/40/70% |
| 弓手 | 後衛物理 | 攻撃射程/速度UP |
| 治癒 | 支援 | 全体に毎秒回復、段階UP |
| 暗殺 | 奇襲 | 開幕、敵後衛に飛び込み追加ダメ |

→ 1編成で「派閥2-3 + クラス2-3」を重ねる戦略深度。

---

## 6. 盤面

- 自陣 **横4 × 縦3 = 12マス**(前列=近接有利 / 後列=遠隔安全)。
- 配置上限 = レベル(2→8体)。ベンチ(控え)8枠。
- 敵(ゴースト/AI)は鏡写しに自動配置。

---

## 7. PvE / ゴースト

- **ゴースト** = 他プレイヤーが過去に組んだ編成スナップショット(Firebase、同期不要)。
- ラウンドの敵抽選: 難易度帯(到達ラウンド)に応じてゴーストプールから抽選。**不足時はAIテンプレ編成にフォールバック**。
- R1-3 は固定AIクリープ(難易度カーブの土台)。R12 はボス(専用編成 or 高レート ゴースト + ボスバフ)。
- 自分の最終編成は試合後に **難易度帯別ゴーストとして保存**(後続プレイヤーの相手になる)。

---

## 8. 勝敗・スコア

- **ラウンド敗北ダメージ** = 生存した敵ユニット数 × 1 + ラウンド係数(R1-3:0 / R4-7:+2 / R8-11:+3 / R12:+5)。
- HP=0 で終了(その時点の到達ラウンドで確定)。R12 勝利で **クリア**。
- **スコア** = 到達ラウンド×100 + 残HP×10 + 累計撃破数 + 達成シナジー段階合計×5 + クリアボーナス500。
- スコア → デイリー/ランキング、少量のガチャ石報酬(アカウント保持)。

---

## 9. データモデル(Firebase, 案)

```
prismboard/
  ghosts/{bucket}/{id}: { units:[{charId,star,pos,clazz}], factions:{id:count}, roles:{id:count}, power:int, ownerName, createdAt }
  scores/{uid}: { best:int, lastRun:{score,round,clear,at}, totalRuns:int }
  ranking/{period}/{uid}: { name, score }
  config: { version, dropRates, factionEffects, roleEffects }   // 調整値はサーバ側に置き再調整しやすく
```
- `bucket` = 難易度帯(到達ラウンド or レート区分)。
- ユニット定義は POOL から派生(`cardgame/cards.json` と同様の派生データ生成スクリプトを流用)。

---

## 10. バランス初期値

- **ステータス換算**: HP = basePower × 12 × ★倍率、ATK = basePower × 1.0 × ★倍率(★1=1.0 / ★2=2.2 / ★3=4.0)。
- **戦闘**: 1秒刻みの自動戦闘。前列から接敵、後列は射程内を攻撃。スキルはマナ(攻撃/被弾で蓄積)満タンで発動。
- **ショップ排出率(レベル別 %、R/SR/SSR/UR/LR)** 初期案:
  - L2: 75/25/0/0/0 / L3: 60/30/10/0/0 / L4: 45/33/20/2/0 / L5: 35/35/25/5/0 / L6: 25/35/28/11/1 / L7: 18/30/32/17/3 / L8: 12/25/33/24/6
- 12ラウンドで決着する想定でHP/ダメージを調整(テストで反復)。

---

## 11. UI / 実装方針(CLAUDE.md 準拠)

- 新ディレクトリ `prismboard/`(`cardgame/` と同様の独立構成: index.html / script.js / style.css / data)。
- **ホーム導線(案A確定)**: カードバトルと同じく、ホームに **専用の1行 `home-prismboard-row` → `/prismboard/`** を追加する(既存 `home-cardgame-row`[index.html L174 付近]の直下に並べる)。`script.js` の cardgame 表示制御(L10671 付近、`home-cardgame-row` を `display:flex`)と同パターンで表示。未登録者はタップでアカウント登録 prompt(`home-cardgame-row` の `_pcbEntry` ロジック L5571 を踏襲)。NEW! バッジ・aurora 等のスタイルも cardgame 行を流用。**この行追加は実ディレクトリ `/prismboard/` が出来てから(Phase 1 以降)** 行う(先に行だけ足すと 404)。ゲームが3本以上に増える場合は「ミニゲームハブ」へ再構成を検討(案B)。
- モーダル3点セット(背景scroll lock + MutationObserver + keydown capture)、Esc閉じ/Tabフォーカストラップ、モバイルタップ領域≥44px、既存 `.btn/.card/.modal` デザイン踏襲。
- データ保持は Firebase(アカウント保持)。未ログイン時はゲスト(ローカル)→ログイン誘導。
- ストーリー世界観と派閥効果・ボスを整合(派閥名・キャラ性は POOL/STORY に準拠)。
- **クラス付与**: 各キャラに1クラスを `title`/`caption` から導出 or 手動タグ(派生データ生成時に確定。要レビュー)。

---

## 12. 段階実装計画

| Phase | 内容 |
|---|---|
| **0 (PoC)** | 5体ユニット + 1派閥シナジー + 3ラウンド + 簡易自動戦闘で「テンポ5-7分」感触確認 |
| **1** | コア: ショップ/配置/3体合成/12ラウンド/経済/HP制(敵はAIテンプレのみ) |
| **2** | シナジー: 優先8派閥 + 6クラス効果、確率テーブル |
| **3** | ゴースト保存/取得(Firebase)、難易度帯抽選、フォールバック |
| **4** | 残り派閥固有効果 / アイテム / ランキング / SE・演出 / ボス |

---

## 13. 未決事項(次段で確定)

- クラス(ロール)6種の最終定義 + 各キャラへの付与ルール(自動導出 vs 手動)。
- 派閥固有効果の具体数値とバランス(優先8 → 全20)。
- ボス(R12)の仕様(専用ボスキャラ or 高レートゴースト+バフ)。
- ガチャ未所持キャラもショップに出るか(= ガチャと独立 or 所持依存)。← 体験設計に直結、要相談。
- 報酬(ガチャ石量・デイリー)の規模。

---

*v1.0 — オートチェス準拠 / PvE・ゴースト / HP制サバイバル / 派閥×ロール2軸。実装は Phase 承認後に着手。*
